{
  "name": "@nicbet/pi-mcp-extension",
  "version": "0.1.1",
  "description": "Expose stdio and Streamable HTTP MCP servers from .mcp.json as Pi tools",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nicbet/pi-mcp-extension.git"
  },
  "bugs": {
    "url": "https://github.com/nicbet/pi-mcp-extension/issues"
  },
  "homepage": "https://github.com/nicbet/pi-mcp-extension#readme",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "index.ts",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --project tsconfig.json",
    "lint": "biome lint index.ts",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun run test"
  },
  "keywords": [
    "pi-package",
    "pi",
    "mcp",
    "model-context-protocol"
  ],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "dependencies": {
    "typebox": "^1.3.16"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/node": "^26.2.0",
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
